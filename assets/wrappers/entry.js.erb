(function () {
    var module = { exports : {} };
    var exports = module.exports;
    var __dirname = <%= MultiJson.encode(dirname) %>;
    var __filename = <%= MultiJson.encode(filename) %>;
    
    var require = function (file) {
      return __require(file, <%= MultiJson.encode(dirname) %>);
    };
    require.modules = __require.modules;
    
    <%= body %>;
})();
